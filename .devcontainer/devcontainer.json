{
    "image": "mcr.microsoft.com/devcontainers/python:3.11-bullseye",
    "customizations": {
        "codespaces": {
            "openFiles": [
                "README.md",
                "Chatbot.py"
            ]
        },
        "vscode": {
            "settings": {},
            "extensions": [
                "ms-python.python",
                "ms-python.vscode-pylance"
            ]
        }
    },
    "forwardPorts": [8501],
    "postCreateCommand": "python3 -m pip install --upgrade pip && python3 -m pip install virtualenv && python3 -m virtualenv .venv && . .venv/bin/activate && pip install -r requirements.txt",
    "postAttachCommand": "bash -c '. .venv/bin/activate && streamlit run Chatbot.py --server.enableCORS false --server.enableXsrfProtection false'",
    "portsAttributes": {
        "8501": {
            "label": "Application",
            "onAutoForward": "openPreview"
        }
    },
    "remoteUser": "vscode",
    "features": {
        "ghcr.io/devcontainers/features/python:1": {
            "version": "3.11"
        }
    }
}
